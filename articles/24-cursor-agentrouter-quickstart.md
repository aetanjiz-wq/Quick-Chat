# Cursor + AgentRouter Quick Start

If you use Cursor and want to test a custom OpenAI-compatible route, AgentRouter can be useful where the client allows a custom provider or base URL.

Start by confirming which Cursor features can use your custom API configuration. Add the AgentRouter key securely, use the OpenAI-compatible endpoint, and select a model supported by your account. Then test a lightweight request before depending on the setup for a major project.

Remember that some Cursor capabilities may still depend on Cursor's own plan or product behavior, so a custom endpoint does not necessarily replace every built-in service.

I personally received $200 in AgentRouter credits when I joined. That amount may not be available to every user.

Referral: https://agentrouter.org/register?aff=rQi3

**Disclosure:** This is my referral link and I may receive a benefit if you sign up through it.